# 后端架构设计（前端驱动版 / API-First）

> 本方案完全基于当前前端 `frontend/src` 的实际功能与数据结构反向推导
> 目标：前端需要什么，后端就提供什么；前端怎么写，接口就怎么设计

---

## 一、设计原则

1. **API-first（前端驱动）**：每个后端服务的接口都从前端页面的交互推导出来
2. **控制平面 / 认知平面分离**（沿用现有架构）：
   - Control Plane：所有 CRUD、认证、会话、SSE 推送、事件持久化
   - Cognition Plane：Agent 推理引擎（LangGraph）、工具执行
3. **SSE 事件协议 = 前端 Step 结构**：前端 `Run Trace` 里展示的每个步骤，就是后端推送的每个事件
4. **前端保持零侵入切换**：当前前端是 mock 数据，接后端时只需把 `axios` 请求指向新 API，数据结构完全兼容

---

## 二、前端功能 → 后端服务映射

| 前端页面 | 关键组件 | 后端服务域 | 说明 |
|---------|---------|-----------|------|
| Agent 列表/配置 | `agents/list.tsx` `agents/config.tsx` | Agent Service | Agent CRUD + 状态机 + 能力关联 |
| 对话调试 | `DebugPanel.tsx` `chat/index.tsx` | Session / Run / SSE | 会话 + 流式执行 + Trace |
| 执行详情面板 | `StepTimeline` `TraceTimeline` | Event Stream | 思考/工具调用/结果全程事件推送 |
| 资源库 | `resources/index.tsx` | Resource Service | 知识库 / Skill / 提示词 / 本体 / 数据源 |
| 评测中心 | `evaluation/index.tsx` | Evaluation Service | 评测集 / 评测任务 / 报告 |
| 空间管理 | `workspace/index.tsx` | Space Service | 空间 / 成员 / 角色 / 邀请 |
| 顶栏 | `TopNav.tsx` | Auth Service | 登录 / 用户信息 / 权限 |

---

## 三、请求链路（一次"边调试边对话"的完整旅程）

```
┌─────────────┐      ┌────────────────────────────────┐      ┌──────────────────┐
│   前端 React  │      │        Control Plane (8000)      │      │  Cognition Plane  │
│   Agent配置页 │      │  ┌──────────┐  ┌─────────────┐   │      │      (8001)       │
│              │─────▶│  │Agent API  │  │ Session API │   │      │  ┌────────────┐  │
│  DebugPanel  │ axios│  │Resource API│ │   Run 入口   │───┼─────▶│  │ LangGraph  │  │
│              │      │  └──────────┘  └──────┬──────┘   │  HTTP │  │ Agent Loop │  │
│              │◀─────│  ┌───────────────────┐│          │  call │  └─────┬──────┘  │
│  Run Trace   │ SSE  │  │  SSE Manager      │◀┼─────────│───────│────────┘         │
│  事件流渲染   │      │  │ (THOUGHT/TOOL/RESULT)│          │      │  工具执行       │
└─────────────┘      └───────────────────────────┘      └──────────────────┘
```

**步骤拆解：**

1. **配置 Agent**：`PUT /api/v1/agents/{id}`（前端"保存"按钮）
2. **调试对话**：`POST /api/v1/sessions` 创建调试会话（携带 agent 配置快照）
3. **发送消息**：`POST /api/v1/sessions/{sid}/runs` with `{ content }`
4. **后端编排**：Control Plane 转发给 Cognition Plane，LangGraph Agent 开始推理循环
5. **实时回传**：Cognition Plane 通过 Redis Stream / HTTP 回调，把执行事件逐条发到 Control Plane 的 SSE Manager
6. **前端渲染**：前端通过 `GET /api/v1/sessions/{sid}/stream` 收 SSE 事件，逐条渲染 Run Trace
7. **历史回溯**：`GET /api/v1/sessions/{sid}/runs/{rid}/events` 拿到完整事件列表重放

---

## 四、API 接口清单（对齐前端每个交互）

### 1. Auth Service `/api/v1/auth`
| 方法 | 路径 | 前端对应 |
|------|------|---------|
| POST | `/login` | 登录 |
| POST | `/register` | 注册 |
| GET | `/me` | 顶栏用户信息 |

### 2. Agent Service `/api/v1/agents`
| 方法 | 路径 | 前端对应 |
|------|------|---------|
| GET | `/` | 列表页（支持 `?keyword=&status=` 筛选） |
| POST | `/` | 新建 Agent |
| GET | `/{id}` | 配置页加载详情 |
| PUT | `/{id}` | 配置页"保存" |
| DELETE | `/{id}` | 列表页删除 |
| POST | `/{id}/publish` | 上架 |
| POST | `/{id}/offline` | 下架 |
| PUT | `/{id}/capabilities` | 添加/移除知识库/本体/Skill |
| GET | `/pools` | 知识库/本体/Skill 可选池 |

### 3. Resource Service `/api/v1/resources`
| 方法 | 路径 | 前端对应 |
|------|------|---------|
| GET | `/?type=kb\|skill\|prompt\|ontology\|ds` | 资源库 5 个 Tab |
| POST | `/?type=` | 新建资源 |
| PUT | `/{id}` | 编辑资源 |
| DELETE | `/{id}` | 删除资源 |

### 4. Session / Chat Service `/api/v1/sessions`
| 方法 | 路径 | 前端对应 |
|------|------|---------|
| POST | `/` | 创建调试/对话会话 |
| GET | `/` | 会话列表 |
| GET | `/{sid}/messages` | 加载历史消息 |
| POST | `/{sid}/runs` | 发送消息 |
| GET | `/{sid}/stream` | **SSE**: 实时执行事件 |
| GET | `/{sid}/runs/{rid}/events` | Run Trace 历史重放 |

### 5. Evaluation Service `/api/v1/evaluations`
| 方法 | 路径 | 前端对应 |
|------|------|---------|
| GET | `/datasets` | 评测集管理 Tab |
| POST | `/datasets` | 新建评测集 |
| GET | `/datasets/{id}/cases` | 评测集用例 |
| POST | `/tasks` | 新建评测任务 |
| GET | `/tasks` | 评测任务列表 |
| GET | `/tasks/{id}/report` | 评测报告（通过率/用例明细） |

### 6. Space Service `/api/v1/spaces`
| 方法 | 路径 | 前端对应 |
|------|------|---------|
| GET | `/` | 空间列表（顶栏领域空间） |
| GET | `/{id}` | 当前空间信息 |
| GET | `/{id}/members` | 空间成员表格 |
| POST | `/{id}/members/invite` | 邀请成员 |
| PUT | `/{id}/members/{uid}` | 停用/启用 |
| GET | `/me` | "返回领域空间" |

---

## 五、SSE 事件协议（与前端 Step 一一对应）

前端 `Step` 结构：
```ts
interface Step {
  nodeType: 'THOUGHT' | 'TOOL' | 'RESULT';
  nodeName: string;      // 节点名，如"数据查询"
  title: string;         // 简短标题
  detail?: string;       // 思考/说明内容
  input?: string;        // 工具入参
  result?: string;       // 工具返回值
  duration?: number;     // 耗时 ms
  status: 'running' | 'success' | 'error';
}
```

后端 SSE 事件协议（逐条推送）：
```
event: run_start        data: { run_id, session_id, agent_id }
event: thought          data: { node_name, content }              → Step(nodeType: THOUGHT, detail)
event: tool_call_start  data: { tool_name, input_json }           → Step(nodeType: TOOL, running)
event: tool_call_end    data: { tool_name, output_json, duration }→ Step(nodeType: TOOL, success)
event: result           data: { content }                         → Step(nodeType: RESULT)
event: run_end          data: { run_id, total_duration }
event: error            data: { message }                         → Step(status: error)
```

**前端消费逻辑不变**：`StepTimeline` / `TraceTimeline` 已经支持 `running → success → error` 三种状态和 `input/result/detail/duration` 全部字段，后端只需按上述协议补全数据即可无缝接入。

---

## 六、数据库表设计（扩展现有）

现有表：`users` `sessions` `messages` `run_events` `tools`

新增表：

```sql
-- Agent 本体
agents(
  id UUID PK,
  name TEXT, model TEXT,
  status ENUM('draft','published','offline'),
  version TEXT, description TEXT,
  system_prompt TEXT,
  created_at, updated_at
)

-- Agent 能力关联（多对多）
agent_capabilities(
  id UUID PK,
  agent_id UUID FK,
  type ENUM('knowledge','ontology','skill'),
  resource_id UUID FK,
  created_at
)

-- 资源池（知识库/Skill/提示词/本体/数据源 统一表）
resources(
  id UUID PK,
  type ENUM('kb','skill','prompt','ontology','ds'),
  name TEXT, description TEXT,
  meta JSONB,            -- 版本号/文档数/接入状态等
  owner_id UUID FK,
  created_at, updated_at
)

-- 空间与成员
spaces(id UUID PK, name TEXT, owner_id UUID FK, created_at)
space_members(
  id UUID PK,
  space_id UUID FK, user_id UUID FK,
  role ENUM('admin','member'),
  status ENUM('active','disabled'),
  joined_at
)

-- 评测
eval_datasets(id UUID PK, name TEXT, owner_id, created_at)
eval_cases(id UUID PK, dataset_id FK, question TEXT, expected TEXT, score)
eval_tasks(
  id UUID PK, name TEXT, agent_id FK,
  status ENUM('pending','running','completed','failed'),
  progress INT, total_cases INT, passed_cases INT,
  report JSONB, created_at
)
```

`run_events` 扩展字段以支持 Trace 详情展示：
```sql
run_events(
  event_id UUID PK,
  run_id UUID, session_id UUID,
  event_type ENUM('THOUGHT','TOOL_CALL','TOOL_RESULT','OBSERVATION','RESULT','ERROR'),
  sequence INT,            -- 排序
  node_name TEXT,
  content JSONB,           -- { detail, input, result }
  duration_ms INT,
  created_at TIMESTAMP
)
```

---

## 七、Cognition Plane 设计（LangGraph 引擎）

```
Cognition Plane (FastAPI, port 8001)
├── app/api/routes/agent.py        # POST /invoke 入口（接收 run 请求）
├── app/services/engine.py         # LangGraph StateGraph 组装
├── app/services/gateway.py        # 工具调用网关（MCP 客户端）
├── app/services/event_emitter.py  # 把图内步骤转成 SSE 事件送回 Control Plane
├── app/services/executor.py       # 脚本/skill 执行器
└── app/graphs/
    ├── react_graph.py             # 思考-行动-观察 循环
    └── plan_execute_graph.py      # 规划-执行（并行子任务）
```

**LangGraph 状态定义（对齐前端 Step）：**
```python
class AgentState(TypedDict):
    messages: Annotated[list, add_messages]
    trace: list[dict]          # [{node_type, node_name, title, detail, input, result, duration, status}]
    current_tool: dict | None
    max_iterations: int = 5
```

**核心循环逻辑：**
```
1. 用户消息 → 状态机 START
2. LLM 调用节点 → 输出"直接回答" 或 "调用工具"（THOUGHT 事件）
3. 工具执行节点 → 通过 MCP 调用工具（TOOL_CALL_START → TOOL_CALL_END 事件）
4. 结果回灌 → 作为新消息再次给 LLM（OBSERVATION 事件）
5. 直到 LLM 直接回答 → END（RESULT 事件）
6. 每一步写入 trace，最终 run_events 持久化到 Control Plane
```

---

## 八、与现有代码的差距清单（实施路线）

现有代码已有：`auth` `sessions` `messages` `runs` `tools` 路由、`sse_manager`、`event_recorder`

**需要新增/改造：**

| 优先级 | 任务 | 模块 |
|--------|------|------|
| P0 | Agent CRUD + 状态机 + 能力关联 | Control Plane 新路由 `agents.py` |
| P0 | Run 串联：前端发送消息 → 调 Cognition → SSE 回推 | Control Plane `runs.py` 改造 + SSE 协议对齐 |
| P0 | Cognition 引擎按 Trace 结构输出事件 | Cognition Plane `engine.py` |
| P1 | Resource 管理（5 类资源统一 CRUD） | Control Plane 新路由 `resources.py` |
| P1 | Space / 成员管理 | Control Plane 新路由 `spaces.py` |
| P1 | Evaluation（评测集/任务/报告） | Control Plane 新路由 `evaluations.py` |
| P2 | 前端接后端：axios baseURL 指向 8000，逐步替换 mock | `frontend/src/utils/` 新增 `request.ts` |
| P2 | LangSmith/tracing 集成，run_events 可视化 | Cognition Plane |

---

## 九、前端接入后端的最小改动

1. 前端加 `src/utils/request.ts`（axios 实例，baseURL `http://localhost:8000/api/v1`）
2. Agent 页：`agentStore` 的 actions 从"本地 state"改为"调 API"
3. 对话页：`send()` 不再走 `mockReply`，改为：
   ```ts
   const resp = await fetch(`/api/v1/sessions/${sid}/stream`, ...)
   const reader = resp.body.getReader()
   // 逐 chunk 解析 SSE 事件 → 调 setMessages 更新 steps
   ```
4. `Step` 数据结构保持不变，Trace 组件零改动

> 前端 `Step` 类型 / `mockChat.ts` 的事件结构就是后端的接口契约，可称之为 "Trace Contract"。

---

## 十、实现进度（已完成 ✅）

> 更新于 2026-09-22。路线图全部实现并通过自动化验证。

| 优先级 | 任务 | 状态 | 代码位置 |
|--------|------|------|---------|
| P0 | Agent CRUD + 状态机 + 能力关联 | ✅ 完成 | `control-plane/app/api/routes/agents.py`、`services/agent_service.py` |
| P0 | Run 串联（发消息 → Cognition → SSE 回推） | ✅ 完成 | `control-plane/app/api/routes/sessions.py`（`/{sid}/runs`、`/{sid}/stream`） |
| P0 | SSE 协议对齐（Trace Contract 转换） | ✅ 完成 | `control-plane/app/services/sse_protocol.py` |
| P0 | Cognition 引擎按 Trace 输出（含 Mock 模式） | ✅ 完成 | `cognition-plane/app/agent/engine.py` |
| P1 | Resource 5 类统一 CRUD + 能力池 | ✅ 完成 | `control-plane/app/api/routes/resources.py`、`services/resource_service.py` |
| P1 | Space / 成员管理 | ✅ 完成 | `control-plane/app/api/routes/spaces.py`、`services/space_service.py` |
| P1 | Evaluation 评测中心（集/用例/任务/报告） | ✅ 完成 | `control-plane/app/api/routes/evaluations.py`、`services/evaluation_service.py` |
| P2 | 前端接后端（axios + SSE 替换 mock，含离线兜底） | ✅ 完成 | `frontend/src/api/`、`utils/request.ts`、`stores/agentStore.ts`、`pages/chat/index.tsx` |
| P2 | LangSmith / tracing 集成 | ⏳ 待做 | Cognition Plane |

### 新增数据表（`create_all` 自动建表）

`agents` `agent_versions` `resources` `spaces` `space_members` `eval_datasets` `eval_cases` `eval_tasks`（另复用原有 `users/sessions/messages/runs/run_events`）

### 验证记录
- **后端端到端测试**：`e2e_test_p0.py`，54 项断言全通过（认证 / Agent CRUD / Run SSE / Resource / Space / Evaluation）
- **前端**：`tsc --noEmit` + `vite build` 通过
- **全链路 CDP 实测**：Agent 列表展示后端真实数据；对话页输入消息后实时渲染后端 SSE 执行 Trace（THOUGHT → TOOL×2 → 结果）

### 开发环境三服务
| 服务 | 地址 |
|------|------|
| 前端 Vite | http://localhost:5173 |
| Control Plane | http://localhost:8080/docs |
| Cognition Plane | http://localhost:8000/docs |

> 启动：`start_control_plane.py` + `start_cognition_plane.py`（需 `aigc_gragh_v1` 环境）+ `frontend: npm run dev`