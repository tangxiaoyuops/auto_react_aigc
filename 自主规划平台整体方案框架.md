# 自主规划平台整体方案框架

## 一、总体技术选型

| 层级 | 技术栈 | 选型理由 |
|------|--------|----------|
| 前端框架 | React 19 + TypeScript + Vite | 提供卓越的类型安全和开发体验，Vite带来极快的构建速度。 |
| UI组件库 | Tailwind CSS + shadcn/ui | 可快速构建现代化、高度可定制的界面。 |
| 后端框架 | FastAPI (Python) | 高性能、异步支持、自动生成API文档，与AI生态（LangChain等）集成最佳。 |
| AI编排层 | LangGraph | 用于构建可控、有状态的Agent工作流，支持ReAct模式、循环和检查点。 |
| 工具协议 | MCP (Model Context Protocol) | 行业标准的工具调用协议，用于集成外部工具和服务。 |
| 数据库 | PostgreSQL + Redis | PG用于持久化存储（会话、事件、知识库），Redis用于缓存和流式状态管理。 |
| 消息队列 | RabbitMQ / Kafka | 用于处理异步任务、长耗时工具调用和系统解耦。 |

---

## 二、总体架构设计

系统采用前后端分离的微服务架构，核心是“控制平面”与“认知平面”的分离。

### 架构分层概览
1. 前端展示层（React 19 + TypeScript + Vite）
2. API网关层（统一入口、路由与鉴权）
3. 控制平面（会话管理、事件持久化、SSE推送；可选Go / Java Spring Boot）
4. 认知平面（Agent大脑：FastAPI + LangGraph）
5. 工具与技能层（MCP工具、Skill脚本）
6. 数据层（PostgreSQL + Redis + 消息队列）

### 核心原则
- 控制与认知分离：控制平面负责流程与数据，认知平面负责推理与行动。
- 流式优先：SSE/WebSocket实时推送运行状态。
- 可重放性：事件以“仅追加”方式存储，支持运行回放与审计。
- 可扩展工具链：通过MCP协议接入外部工具与技能。

---

## 三、前端设计与实现

前端需提供对话交互与过程可视化两大核心能力。

### 1. 核心功能模块
- 对话界面（Chat Interface）：消息列表、输入框、模型/工具选择器。
- 过程记录面板（Process Timeline）：展示Agent的“思考→行动→观察”全流程。
- 工具调用可视化：以折叠卡片或时间线形式，展示工具名称、输入参数、执行状态（进行中/成功/失败）和输出结果。
- 状态管理：使用 useReducer 或 zustand 管理对话状态、流式消息和工具调用状态。

### 2. 流式通信与事件协议
- 前端与后端通过 Server-Sent Events (SSE) 或 WebSocket 进行通信，接收流式事件。
- 推荐协议：AG-UI Protocol
  - TEXT_MESSAGE_*：渲染流式文本（类似打字机效果）。
  - REASONING_*：渲染模型的思考过程（ThinkingBlock）。
  - TOOL_CALL_*：渲染工具调用的生命周期。

### 3. 页面路由规划
- `/` 或 `/playground`：主对话工作台。
- `/runs`：历史运行记录列表与详情查看。
- `/dashboard`：平台概览与实时指标。

---

## 四、后端架构设计

后端遵循“控制平面与认知平面分离”的设计模式。

### 1. 控制平面 (Control Plane)
- 职责：负责会话管理、认证鉴权、事件持久化和SSE推送。
- 技术实现：可选用 Go 或 Java (Spring Boot)。
- 核心模块：
  - 会话服务：管理 session_id、thread_id，与前端保持长连接。
  - 事件记录器：将所有执行事件（思考、工具调用、结果）以“仅追加”方式存入PostgreSQL，实现运行可重放。
  - 认证服务：处理用户登录、JWT颁发与校验。

### 2. 认知平面 (Cognition Plane)
- 职责：作为Agent的“大脑”，执行推理循环、调用工具、处理技能。
- 技术实现：Python + FastAPI + LangGraph。
- 核心引擎 - LangGraph Agent：
  - 定义状态图（StateGraph），实现 ReAct 模式（推理-行动-观察循环）。
  - 支持 Plan-Execute 模式，将复杂任务分解为多个子任务并行执行。
  - 支持 Human-in-the-Loop (HITL)，在需要人工确认时中断执行。

### 3. 工具与技能系统 (Tool & Skill System)
- Tool（工具）：原子能力，如“联网搜索”、“计算器”。
- Skill（技能）：更高层的封装，包含 SKILL.md（工作流描述）、多个Tools、脚本和规则。
- 实现原则：“模型负责判断和编排，脚本负责确定性执行”。

---

## 五、核心模块详细设计

### 1. Agent 执行流程
1. 前端发送用户消息。
2. 控制平面创建运行记录，将请求路由至认知平面。
3. LangGraph Agent启动，将用户消息与系统提示词（含可用工具/技能列表）发给LLM。
4. LLM决定行动：直接回答或调用工具/技能。
5. 若调用工具，Agent通过MCP客户端执行，或加载Skill并执行其脚本。
6. 工具/Skill执行结果返回Agent，作为新消息再次发给LLM。
7. 循环直至LLM输出最终答案（达到最大迭代次数或满足停止条件）。
8. 最终答案通过SSE流式返回前端。

### 2. 数据库设计

#### sessions 表
- 存储会话元数据
- 字段：session_id, user_id, title, created_at

#### messages 表
- 存储对话消息
- 字段：message_id, session_id, role, content

#### run_events 表
- 核心表，存储每个运行的详细事件
- 字段：event_id, run_id, session_id, event_type (THOUGHT/TOOL_CALL/TOOL_RESULT/OBSERVATION), content (JSONB), sequence, created_at

#### tools / skills 表
- 存储注册的工具和技能元数据
- 字段：名称、描述、参数schema、MCP端点等

---

## 六、实施路线图

### 阶段一：MVP核心功能（预计4-6周）
- Week 1-2：
  - 搭建React前端脚手架与基础UI组件
  - 实现FastAPI基础框架与会话管理API
  - 设计并初始化数据库表结构
- Week 3-4：
  - 集成LangGraph基础Agent（ReAct模式）
  - 实现SSE流式通信
  - 完成2-3个核心工具（如搜索、计算器）
- Week 5-6：
  - 实现过程可视化面板
  - 用户认证与权限管理
  - 基础测试与性能优化

### 阶段二：扩展与优化（预计6-8周）
- 工具生态系统建设
- 高级Agent模式（Plan-Execute、HITL）
- 性能监控与告警
- 安全加固与审计

### 阶段三：生产化（预计4-6周）
- 多租户支持
- 高可用与灾备方案
- 运维自动化与文档完善

---

## 七、关键设计决策与最佳实践

### 1. 为什么选择“控制与认知分离”？
- 降低耦合：控制平面稳定，认知平面可快速迭代。
- 技术栈匹配：Python生态更适合AI推理，Go/Java更适合高并发网络服务。
- 故障隔离：认知平面错误不影响控制平面的会话与事件记录。

### 2. 工具与技能的分层设计
- Tool：原子、无状态、可组合。
- Skill：编排多个工具，包含业务逻辑与上下文。
- 通过MCP协议统一调用方式，便于扩展。

### 3. 可重放性设计
- 所有事件以仅追加方式存储。
- 支持按run_id回放完整执行链。
- 便于调试、审计与优化。

### 4. 流式优先
- 实时反馈：用户可看到Agent的“思考过程”。
- 降低超时风险：长任务分阶段返回结果。
- 更好的用户体验：打字机效果、工具调用进度条。

---

## 八、风险与应对策略

### 1. 技术风险
- LLM不稳定：实现多模型备份与降级策略。
- 工具调用失败：重试机制与错误隔离。
- 高并发压力：异步队列与限流保护。

### 2. 业务风险
- 用户滥用：速率限制与行为监控。
- 敏感数据泄露：数据脱敏与权限控制。
- 成本失控：Token使用监控与预算控制。

### 3. 运维风险
- 系统故障：多区域部署与自动故障转移。
- 数据丢失：定期备份与恢复演练。
- 安全漏洞：定期安全审计与依赖更新。

---

## 九、成功指标

### 1. 技术指标
- API响应时间 < 200ms（P95）
- 系统可用性 > 99.9%
- Agent平均完成率 > 85%

### 2. 业务指标
- 用户满意度 > 4.5/5
- 工具调用成功率 > 90%
- 系统平均响应延迟 < 3s

### 3. 运维指标
- 故障恢复时间 < 15min
- 部署频率 > 1次/周
- 安全事件 < 1次/季度

---

## 十、后续扩展方向

### 1. 多Agent协作
- 主从Agent模式
- 专家Agent池
- 投票与共识机制

### 2. 知识图谱集成
- 实体识别与关系抽取
- 图谱查询与推理
- 知识更新与维护

### 3. 自适应学习
- 用户行为分析
- Agent能力优化
- 推荐工具与技能

### 4. 企业级特性
- 多租户隔离
- 细粒度权限控制
- 审计与合规报告

---

## 十一、总结

本平台通过“控制与认知分离”的架构设计，结合LangGraph的强大编排能力和MCP的标准化工具协议，构建了一个可扩展、可重放、可观测的自主规划系统。核心优势包括：

1. **灵活性**：支持多种Agent模式和工具组合。
2. **可观测性**：全流程事件记录与可视化。
3. **可扩展性**：模块化设计，易于新增功能。
4. **生产就绪**：完整的认证、监控、容错机制。

该架构为未来集成更复杂的AI能力（如多Agent协作、知识图谱、自适应学习）奠定了坚实基础。
